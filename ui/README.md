# DKI Chat UI

基于 Vue 3 + Element Plus 的 DKI 系统前端界面。

## 功能特性

- 🔐 **用户认证** - 登录/注册功能
- 💬 **智能聊天** - 支持 Markdown 渲染的聊天界面
- ⚙️ **偏好管理** - 用户偏好的增删改查
- 📊 **会话管理** - 历史会话的查看和管理
- 📈 **系统统计** - DKI 系统运行状态监控（需管理员密码）
- 🎨 **主题切换** - 支持浅色/深色主题
- 🌐 **多语言** - 支持中文/英文

## 技术栈

- **框架**: Vue 3 + TypeScript
- **构建工具**: Vite
- **UI 组件**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router
- **图表**: ECharts + vue-echarts
- **Markdown**: markdown-it + highlight.js

## 快速开始

### 安装依赖

```bash
cd ui
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

## 配置

### 环境变量

复制 `env.example` 为 `.env` 并修改配置：

```bash
# API 地址
VITE_API_BASE_URL=/api

# 统计页面密码
VITE_STATS_PASSWORD=your_password
```

### 代理配置

开发模式下，API 请求会代理到后端服务器。在 `vite.config.ts` 中配置：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}
```

## 项目结构

```
ui/
├── public/              # 静态资源
├── src/
│   ├── assets/          # 样式和图片
│   │   └── styles/      # SCSS 样式文件
│   ├── components/      # 通用组件
│   │   ├── ChatInput.vue
│   │   ├── MessageItem.vue
│   │   └── SettingsDialog.vue
│   ├── config/          # 应用配置
│   ├── layouts/         # 布局组件
│   │   └── MainLayout.vue
│   ├── router/          # 路由配置
│   ├── services/        # API 服务
│   │   └── api.ts
│   ├── stores/          # Pinia 状态管理
│   │   ├── auth.ts
│   │   ├── chat.ts
│   │   ├── preferences.ts
│   │   ├── settings.ts
│   │   └── statsAuth.ts
│   ├── types/           # TypeScript 类型定义
│   ├── utils/           # 工具函数
│   │   └── markdown.ts
│   ├── views/           # 页面组件
│   │   ├── LoginView.vue
│   │   ├── ChatView.vue
│   │   ├── PreferencesView.vue
│   │   ├── SessionsView.vue
│   │   └── StatsView.vue
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 页面说明

### 登录页面 (`/login`)

- 用户登录/注册
- 演示模式支持任意用户名密码

### 聊天页面 (`/`)

- 主聊天界面
- 支持 Markdown 渲染
- DKI 调试信息面板
- 快捷提示词

### 偏好管理 (`/preferences`)

- 用户偏好列表
- 按类型/分类筛选
- 偏好的增删改查
- 优先级设置

### 会话管理 (`/sessions`)

- 历史会话列表
- 会话搜索
- 会话详情预览
- 导出/删除功能

### 系统统计 (`/stats`)

- **需要管理员密码验证**
- DKI 运行统计
- 缓存命中率
- 请求趋势图表

## 统计页面鉴权

统计页面使用简单的密码保护（非生产标准）：

1. 在配置文件中设置密码：
   ```typescript
   // src/config/index.ts
   statsAuth: {
     enabled: true,
     password: 'your_password',
   }
   ```

2. 或通过环境变量设置：
   ```
   VITE_STATS_PASSWORD=your_password
   ```

3. 访问统计页面时需要输入密码验证

## 开发说明

### 添加新页面

1. 在 `src/views/` 创建页面组件
2. 在 `src/router/index.ts` 添加路由
3. 如需侧边栏导航，在 `MainLayout.vue` 的 `navItems` 中添加

### 添加新 API

1. 在 `src/types/index.ts` 定义类型
2. 在 `src/services/api.ts` 添加 API 方法
3. 在对应的 store 中调用

### 自定义主题

修改 `src/assets/styles/variables.scss` 中的变量：

```scss
// 主色调
$primary-color: #10b981;

// 浅色主题
$bg-color-light: #ffffff;

// 深色主题
$bg-color-dark: #0f172a;
```

## License

MIT
