# DKI - Dynamic KV Injection System
# Attention-Level User Memory Plugin for LLMs
# Requirements

# ============ Core Dependencies ============
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0

# ============ Inference Engines ============
vllm>=0.2.0
# llama-cpp-python>=0.2.0  # Optional for llama.cpp

# ============ Embeddings & Vector Store ============
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4  # Use this for GPU acceleration

# ============ Database (Config-Driven Adapter) ============
# SQLAlchemy for dynamic table mapping
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Database Adapters (install as needed for upstream app database)
asyncpg>=0.29.0  # PostgreSQL async (recommended)
# psycopg2-binary>=2.9.0  # PostgreSQL sync
# aiomysql>=0.2.0  # MySQL async
# pymysql>=1.1.0  # MySQL sync

# Redis (optional - for L2 preference cache)
redis>=5.0.0  # Async Redis support included

# ============ Web Server & API ============
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
jinja2>=3.1.2
aiofiles>=23.0.0
aiohttp>=3.9.0

# ============ Configuration ============
pyyaml>=6.0.0
python-dotenv>=1.0.0

# ============ Dynamic Vector Search ============
# BM25 for hybrid search (BM25 + embedding reranking)
rank-bm25>=0.2.2

# ============ Metrics & Evaluation ============
rouge-score>=0.1.2
nltk>=3.8.0
sacrebleu>=2.3.0
scikit-learn>=1.3.0

# ============ Utilities ============
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
loguru>=0.7.0
pydantic>=2.0.0
httpx>=0.25.0

# ============ Development & Testing ============
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0

# ============ Visualization (optional) ============
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0
