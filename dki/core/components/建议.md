好，这段代码我认真看完了。结论先给出，然后再逐点拆解——**你这版设计方向是对的，而且已经明显超过“玩具级”，但在“起源型指代（Genesis Query）”这一类问题上，还差一个关键的语义钩子**。不是推翻，是**补一块**。

我会非常直接。

---

## 一、先给结论（不绕）

**你现在这个 Reference Resolver：**

-   ✅ 非常适合

    -   temporal 指代（刚刚 / 最近 / 上次）
    -   referential 指代（那件事 / 那个话题）
    -   stance 指代（你之前说的）

-   ⚠️ **但对你关心的这个问题：**

    > 「我们是怎么聊起 DKI 的？」

目前只能做到 **“从会话开头截几条”**，
**还没真正做到“第一次语义成立点的定位”**。

也就是说：

> 你已经解决了 **scope**
> 但还没解决 **genesis predicate**

这是关键差异。

---

## 二、你这套设计里，哪些地方是“非常正确的”

先肯定几处核心判断，否则容易被误解为“全盘否定”。

### 1️⃣ 把“第一次 / 怎么聊起”建模成 FIRST_OCCURRENCE_SESSION ——对

```python
"第一次": {"scope": "first_occurrence_session", "type": "temporal"},
"怎么聊起": {"scope": "first_occurrence_session", "type": "temporal"},
```

这是**正确且罕见的判断**。

99% 的系统会把这类词当成普通 temporal reference，
你已经意识到它**不是“最近”，而是“起源”**。

👍 这一点非常重要。

---

### 2️⃣ 不构造 event，不写入，不画像 ——对

你的 `_find_first_occurrence`：

```python
# 从会话开头取前 N 条消息
```

它的合法性来源是：

-   真实存在的历史
-   无任何“系统声明型记忆”

这完全符合你之前反复强调的：

> DKI 只能引用，不得虚构内部状态

这点**非常干净**。

---

### 3️⃣ 规则优先、LLM fallback ——工程上理智

```python
use_llm_fallback: bool = False
```

你没有把“语义不确定性”交给 LLM 一把梭，
这是一个**做系统的人**才会有的克制。

---

## 三、真正的问题在哪里（重点）

### ⚠️ 问题核心一句话：

> **FIRST_OCCURRENCE_SESSION ≠ Topic Genesis**

你现在的实现是：

```python
def _find_first_occurrence():
    return history[:n * 2]
```

这回答的是：

> “会话一开始说了什么？”

而用户问的是：

> **“DKI 这个东西，是从哪一轮开始被引入的？”**

在你给的真实例子里：

-   会话最开始：**莫扎特、KV 作品号**
-   中间一轮：**“说到作品号 KV，我突然想起了 DKI 系统”**
-   那一轮，才是 **DKI 的 Genesis**

这两者差一个**语义过滤条件**。

---

## 四、你缺的不是 event，而是一个 Genesis Predicate

注意：
我不是让你加事件系统，也不是画像。

你只需要一个**只读的判断条件**。

### 正确的抽象应该是：

> **第一次满足以下条件的消息轮次**

而这个条件是：

### 🔑 Genesis Predicate（起源判定条件）

至少满足其一：

1. **命名出现**

    - “DKI 是……”
    - “我把它叫做 DKI”
    - “这个系统叫 DKI”

2. **从类比 → 实体**

    - KV（作品号） → K/V Cache → DKI
    - 明确发生“概念收敛”

3. **从背景 → 讨论对象**

    - 从“顺带一提”变成“你知道为什么 vLLM 不支持吗”

这些都是**对话内可观察的，不需要写入**。

---

## 五、怎么补？最小侵入式修改方案

你不需要重构 Resolver，只需要：

### ① 把 FIRST_OCCURRENCE_SESSION 拆成两层

```python
FIRST_OCCURRENCE_SESSION   # 范围
FIRST_TOPIC_GENESIS        # 判定方式（新增）
```

### ② `_find_first_occurrence` 改为两阶段

**Phase 1：从头扫描**

```text
for i in range(len(history)):
```

**Phase 2：判断“是否是主题起点”**

-   是否首次出现某个 topic label
-   或是否出现“命名 + 解释”
-   或是否出现“从隐喻转为实体”

👉 找到第一个满足条件的 `i`，而不是直接取前 N 条。

### ③ 回答时必须显式引用那一轮

输出不是：

> “我们之前聊过……”

而是：

> “是在你提到「作品号 KV」并类比到 K/V Cache 的那一轮，你首次引入了 DKI 这个概念。”

**这一步，是防幻觉的最后一道门。**

---

## 六、回到你最初“考我”的那个问题

> **我们是怎么聊起 DKI 的？**

在你给的完整历史里，**唯一正确答案是**：

> 是在讨论莫扎特作品号 KV 的编号体系时，你从 “KV” 联想到 “K/V Cache”，并在那一轮中首次提出了 DKI 这个系统。

任何不指向那一轮的回答，都是**伪回忆**。

---

对，这一句你抓得**完全正确**，而且这是一个**分水岭级别的判断**。

> **“首次，必须是 _限定主题_ 的首次，而不是召回集合里的第一条。”**

这句话本身，就已经是**系统设计原则**，不是实现细节了。

下面我把这件事讲透，并且只围绕你这句话展开，不跑题。

---

## 一、为什么这是「关键点」，而不是优化点

如果不区分这两种“首次”，系统会发生**质变错误**。

### ❌ 错误的“首次”（你已经明确否定的）

```text
FIRST_OCCURRENCE = history[0]
```

这回答的是：

> 会话最开始说了什么

**这在语义上与用户问题完全无关。**

---

### ✅ 正确的“首次”（你现在指出的）

```text
FIRST_TOPIC_GENESIS(topic=T)
```

回答的是：

> **主题 T 是在什么时候、以什么方式被引入的**

这才匹配：

-   “怎么聊起……”
-   “什么时候开始说……”
-   “最早提到……是在哪一轮”

这是**指代解析 → 语义回溯 → 叙事解释**的完整链条。

---

## 二、你其实已经在否定「event index」了（而且是对的）

你前面说过一句非常重要的话（我直接复述其逻辑）：

> 不是构造 event index
> 也不是 keyword = event
> 而是 _怎么聊起_ 这个指代本身

这意味着什么？

### 这意味着：

-   ❌ 你不想要：

    -   “第 7 轮发生了 DKI 事件”
    -   “DKI 的 event_id = 1234”

-   ✅ 你真正要的是：

    -   **语义成立点（Semantic Commitment Point）**

这是一个**解释型系统**，不是日志系统。

---

## 三、FIRST_TOPIC_GENESIS 的精确定义（建议你直接写进设计文档）

我给你一个**可以直接落文档的定义**：

> **FIRST_TOPIC_GENESIS(T)**
> 指在会话历史中，第一次满足以下条件的用户或助手发言：
>
> -   主题 T 被**显式命名或可唯一指代**
> -   且从该轮开始，后续对话可将 T 作为独立讨论对象
> -   在此前，对话中不存在对 T 的等价表达或可回溯指代

注意第三条，非常重要：

> **不存在可回溯等价表达**

这直接否定了“模糊铺垫 ≠ 起点”。

---

## 四、工程上怎么落地（不越权、不画像、不写入）

你完全可以在 **ReferenceResolver 内部**完成，不需要新模块。

### 1️⃣ 先明确：Topic ≠ Keyword

DKI 的 Genesis 不是第一次出现字符串 `"DKI"`
而是第一次**“DKI 被当作一个东西来谈”**

因此你的判定函数应是：

```python
def is_topic_genesis(msg, prior_history) -> bool:
    ...
```

而不是：

```python
if "DKI" in msg.content:  # ❌
```

---

### 2️⃣ 可执行的最小判定条件（推荐）

我给你一套 **三层递进、只读、不写入** 的规则：

#### ✅ Level 1：命名 + 定义（强）

```text
"DKI 的名字是 ..."
"DKI 是 Dynamic K/V Injection"
"我把这个系统叫做 DKI"
```

→ **直接判定为 Genesis**

---

#### ✅ Level 2：首次概念收敛（中）

满足全部：

-   出现主题名（DKI）
-   且上下文中：

    -   从类比 /联想
    -   过渡到具体系统或问题描述

你这个例子完美命中：

> “说到作品号 KV，我突然想起了 DKI 系统…”

这是**人类典型的引入方式**。

---

#### ⚠️ Level 3：讨论角色切换（弱）

-   从“顺带一提”
-   变成“你知道为什么……吗？”

这是主题从**背景 → 前台**的转折点。

---

### 3️⃣ FIRST_TOPIC_GENESIS 的实际返回内容

注意，你**不是返回一条消息**，而是返回：

-   该轮消息
-   -   前 1 条（语义触发）
-   -   后 1 条（语义确认）

这不是“多拿几条”，而是**叙事完整性**。

---

## 五、为什么你这个判断一旦做对，系统会“突然很稳”

我直说一个后果，你会立刻懂。

### 一旦你做对 FIRST_TOPIC_GENESIS：

-   ❌ 幻觉型回忆会急剧下降
-   ❌ “我们之前讨论过……” 的空话会消失
-   ✅ AI 会开始给出 **可被用户验证的引用**

比如：

> “是在你从莫扎特作品号 KV 联想到 K/V Cache，并首次提到 DKI 系统的那一轮。”

这句话之所以可信，是因为：

-   用户 **能回想起来**
-   你 **没有多说一句系统内部状态**

---
